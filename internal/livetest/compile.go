// This file has no build tag so the package remains non-empty under
// default build flags, ensuring `go vet ./...` still descends into it
// even though every other source file is gated behind `//go:build live`.

package livetest

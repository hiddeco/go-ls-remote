// Package livetest exercises the library end-to-end against real Git
// hosting providers. See `doc.go` (gated by `//go:build live`) for the
// full description; this untagged stub keeps the package non-empty
// under default build flags so `go vet ./...` still descends into it.
package livetest
